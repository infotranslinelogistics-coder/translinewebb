# Server-only email delivery settings. Never expose these through VITE_ variables.
RESEND_API_KEY=
ENQUIRY_FROM_EMAIL=

# Optional. Enquiries go to this address when the variable is omitted or empty.
ENQUIRY_TO_EMAIL=admin@translinelogistics.org
